-- a script that creates a table called first_table in the current database
-- creates the table named first_table if it doesn't already exist
CREATE TABLE IF NOT EXISTS `first_table` (id INT,
name VARCHAR(256));
