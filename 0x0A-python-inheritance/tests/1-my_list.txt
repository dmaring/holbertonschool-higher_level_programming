The ``1-my_list`` module
======================

Using ``print_sorted``
-------------------

A function that prints the list, but sorted (ascending sort):

First import ``print_sorted`` from the ``1-my_list.py`` module:
>>> print_sorted = __import__('1-my_list).print_sorted

* Argument passed must be an int
>>> text_indentation(float('nan'))
Traceback (most recent call last):
    ...
TypeError: text must be an int

# * Text with . ? : should print two newlines after
# >>> text_indentation("Hello there.")
# Hello there.
# <BLANKLINE>

# * Text with . ? : should print two newlines after
# >>> text_indentation("Hello there?")
# Hello there?
# <BLANKLINE>

# * Text with . ? : should print two newlines after
# >>> text_indentation("Hello there:")
# Hello there:
# <BLANKLINE>

# * Text with numbers should be excepted
# >>> text_indentation("e43")
# e43

# * Function should print no white space at beginning or end of line
# >>> text_indentation("   Hello   ")
# Hello

# * Function should print no white space at beginning or end of line
# >>> text_indentation("   \t  ")

# * Function should
# >>> text_indentation("Hi?\n\nThere")
# Hi?
# <BLANKLINE>
# There
